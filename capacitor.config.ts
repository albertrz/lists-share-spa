import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sharedlists.app',
  appName: 'lists-share-spa',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
